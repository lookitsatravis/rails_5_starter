import { shallow } from '@vue/test-utils';
import App from '../../../app/javascript/packs/App.vue';

describe('App.vue', () => {
  it('is so, so true, man.', () => {
    // const wrapper = shallow(App);
    // expect(wrapper.find('p').text()).to.eq('Hello World!');
    expect(true).to.eq(true);
  })
})
